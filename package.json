{
  "scripts": {
    "clean": "rm -rf cache artifacts frontend/public/contract/deployed-addresses.json frontend/src/contract/deployed-addresses.json",
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "start": "bash scripts/clean-start.sh"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.22.0"
  }
}
